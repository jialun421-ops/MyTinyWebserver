# 压测记录

## 环境

- 虚拟机：VMware + Ubuntu 22.04
- CPU：单核
- 内存：2GB
- 服务器配置：线程数 8，ET 模式，端口 9006
- 静态资源：root/index.html（424 字节）
- 压测工具：ApacheBench 2.3

## 测试1：中并发

命令：`ab -n 10000 -c 100 http://127.0.0.1:9006/`

| 指标 | 数值 |
|---|---|
| QPS | 9296 |
| 平均延迟 | 10.8 ms |
| 90% 延迟 | 19 ms |
| 99% 延迟 | 29 ms |
| 失败请求 | 0 |
| Non-2xx 响应 | 0 |

## 测试2：高并发

命令：`ab -n 20000 -c 500 http://127.0.0.1:9006/`

| 指标 | 数值 |
|---|---|
| QPS | 5186 |
| 平均延迟 | 96.4 ms |
| 90% 延迟 | 140 ms |
| 99% 延迟 | 353 ms |
| 失败请求 | 0 |
| Non-2xx 响应 | 0 |

## 分析

- **吞吐**：中并发 9296 QPS，高并发 5186 QPS，随并发升高吞吐下降
- **延迟**：从 c=100 的 10.8 ms 增长到 c=500 的 96.4 ms，符合排队论预期
- **稳定性**：两轮测试均无失败请求，连接不丢失
- **瓶颈**：虚拟机单核 CPU 是主要瓶颈，高并发下线程调度与上下文切换开销显著
- **优化点**：
  - `listen` 的 backlog 从 5 调整为 `SOMAXCONN`，解决了高并发下连接被丢弃的问题
  - 兼容 HTTP/1.0，使 ab 等客户端能正常压测

## 已知限制

- 未做线程数对比实验（可后续补）
- 未测试长连接（keep-alive）模式下的吞吐
- 未测试大文件传输性能
